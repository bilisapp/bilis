/**
 * The one script the marketing pages load.
 *
 * Blade only, no Inertia: each module below finds its own hook in the markup
 * and does nothing when that hook is absent, so a page pays for what it uses.
 */
import './hero-shader';
import './live-tail';
import './copy';
